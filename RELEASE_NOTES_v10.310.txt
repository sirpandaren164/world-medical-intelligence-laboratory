MIW v10.310 — Onconomics + METASTAT Booklet Recovery

Root-cause fixes
1. Mixed-PDF patient linkage
- Onconomics Review now accepts the verified parent-document patientId.
- The Onconomics source snapshot is persisted to the same patient as the verified mixed PDF.
- Existing Onconomics reports are also linked to the Booklet by parentDocumentId, so an older sibling/duplicate patient record cannot hide the assay.

2. Existing database recovery
- If a mixed PDF already contains stored Onconomics pages but its oncoReport envelope is absent, Patient Booklet creates a non-destructive virtual Onconomics Plus report from the stored PDF Text Layer.
- Recovery restores report summary / CTC and source-grounded structured rows that can be recovered without inventing chart values.
- Full chart OCR remains preferred when the original PDF is available.

3. METASTAT reading order
- Primary destination trend printed by METASTAT is ranked first when present.
- Remaining organ groups are ranked by UP REGULATED / supportive marker pattern.
- The UI explicitly states that this is an organ-tropism reading aid, not a true probability of metastasis.

Regression target
- ilovepdf_merged: Onconomics Plus pages 1–20 + METASTAT pages 21–24.
- Expected Patient Booklet: at least 2 specialised reports: Onconomics Plus + METASTAT.
